#!/usr/bin/env bun
import { formatError } from "../src/core/error.ts";
import { main } from "../src/core/main.ts";

main()
  .catch(async (e) => {
    console.error(formatError(e));
    process.exit(1);
  })
  .then((code) => {
    if (typeof code === "number") {
      process.exit(code);
    }
  });
