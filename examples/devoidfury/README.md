# devoidfury's config

These are my config files, I use these on my daily driver.

## My server setup

For an AI host I've got a dedicated AMD strix halo 128GB mini-pc running linux, cachyos - headless no desktop / graphical mode installed.

### Intranet Model Gateway Server: llama-swap

I run llama-swap with a version of the [llama-swap-config.yaml](./llama-swap-config.yaml) -- make sure to set an API key if you use this!

None of those will auto-download -- my workflow is to d/l these manually and then write the config by hand -- but it should give a good idea of decent settings for each of these on this box.
