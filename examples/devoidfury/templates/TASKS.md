
# Instructions

This is a living document designed for long-horizon tasks. Follow the instructions and update it according to your result.

You only need to complete one actionable task in this session, so find one and take your time with it, be thorough.

Before you begin, read these files for project context:

- package.json
- AGENTS.md

(1) If the (Review) list is empty, skip to (2). Check the (Review) list and pick an item to review. Make sure it is complete, changes exist, that there is a real test, and that the change has value. Trim any unnessary code comments that don't add value or explain "why". When you verify the change is good, move it to the (Verified) list; if it has serious issues that you cannot fix, move it to the (Suspect) list instead.

(2) Pick an item from the (Todo) list. It is a raw excerpt from a research finding -- paraphrase it to a concise issue/action, verify it is a real issue or improvement and that addressing it is worthwhile, if not move it to the (Suspect) list with a summary of the holdup and stop; otherwise write a test and then implement the solution. Double check that all the tests pass, the files have been edited the way you expected, and other checks pass. When you're finished, move the item to the (Review) list; or if you cannot solve then move it to (Suspect) with explanation. If the (Todo) list is empty, try and improve the test coverage (Do not add coverage work notes to this document).

# (Verified)

# (Suspect)

# (Review)

# (Todo)
