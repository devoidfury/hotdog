{
  "name": "hotdog",
  "version": "0.1.4",
  "main": "src/core/index.js",
  "dependencies": {},
  "devDependencies": {},
  "bin": {
    "hotdog": "bin/hotdog"
  },
  "description": "AI agent harness with tool calling support. Zero dependencies, just bun.",
  "scripts": {
    "start": "bun bin/hotdog",
    "test": "bun test --only-failures --coverage"
  },
  "type": "module",
  "engines": {
    "bun": ">=1.0"
  }
}
