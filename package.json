{
  "name": "oa-agent",
  "version": "0.1.0",
  "description": "AI agent harness with tool calling support",
  "type": "module",
  "main": "src/lib.js",
  "bin": {
    "oa-agent": "bin/oa-agent"
  },
  "scripts": {
    "start": "bun src/main.js",
    "check": "bun lint",
    "lint": "bunx --bun eslint src/"
  },
  "devDependencies": {
    "eslint": "^9.0.0"
  }
}
