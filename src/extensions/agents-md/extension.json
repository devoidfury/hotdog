{
  "name": "agents-md",
  "provides": [],
  "loadOrder": 10,
  "description": "Loads AGENTS.md and contributes Project Context section to the system prompt",
  "configSchema": {
    "agentsMd": {
      "type": "object",
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": true,
          "description": "Whether this extension is enabled. Set to false to disable the extension entirely."
        },
        "autoload": {
          "type": "boolean",
          "default": true,
          "description": "Whether to automatically load AGENTS.md from CWD. When false, the extension loads but skips reading the file."
        }
      },
      "additionalProperties": false,
      "layers": [
        { "source": "config", "key": "agentsMd" },
        { "default": {} }
      ]
    }
  }
}
