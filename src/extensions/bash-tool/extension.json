{
  "name": "bash-tool",
  "provides": ["tools"],
  "loadOrder": 1,
  "description": "Bash tool — execute shell commands",
  "configSchema": {
    "bashTool": {
      "type": "object",
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": true,
          "description": "Whether this extension is enabled. Set to false to disable the extension entirely."
        },
        "bashTimeoutMs": {
          "type": "number",
          "default": 60000,
          "description": "Default timeout for bash commands in milliseconds"
        },
        "maxTimeoutMs": {
          "type": "number",
          "default": 600000,
          "description": "Hard cap on a model-requested timeoutMs in milliseconds"
        },
        "maxToolOutputLines": {
          "type": "number",
          "default": 600,
          "description": "Maximum number of output lines for tool results"
        },
        "env": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra environment variables for commands the model runs (variable names are preserved verbatim)"
        }
      },
      "additionalProperties": false,
      "layers": [
        { "source": "config", "key": "bashTool" },
        { "default": {} }
      ]
    }
  }
}
