{
  "name": "environment",
  "provides": [],
  "loadOrder": 10,
  "description": "Contributes Environment section to the system prompt",
  "configSchema": {
    "environment": {
      "type": "object",
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": true,
          "description": "Whether this extension is enabled. Set to false to disable the extension entirely."
        }
      },
      "additionalProperties": false,
      "layers": [
        { "source": "config", "key": "environment" },
        { "default": {} }
      ]
    }
  }
}
