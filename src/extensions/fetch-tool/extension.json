{
  "name": "fetch-tool",
  "provides": ["tools"],
  "loadOrder": 1,
  "description": "Make HTTP requests via the fetch tool",
  "configSchema": {
    "type": "object",
    "properties": {
      "enabled": {
        "type": "boolean",
        "default": true,
        "description": "Whether this extension is enabled. Set to false to disable the extension entirely."
      },
      "toolEnabled": {
        "type": "boolean",
        "default": true,
        "description": "Whether to register the fetch tool",
        "layers": [
          { "source": "config", "key": "fetchToolEnabled" },
          { "default": true }
        ]
      }
    },
    "additionalProperties": false
  }
}
