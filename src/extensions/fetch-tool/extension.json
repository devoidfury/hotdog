{
  "name": "fetch-tool",
  "provides": ["tools"],
  "loadOrder": 1,
  "description": "Make HTTP requests via the fetch tool",
  "configSchema": {
    "fetchTool": {
      "type": "object",
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": true,
          "description": "Whether this extension is enabled. Set to false to disable the extension entirely."
        },
        "maxBodyLength": {
          "type": "number",
          "default": 8000,
          "description": "Maximum number of characters to return before truncating responses."
        },
        "fetchTimeoutMs": {
          "type": "number",
          "default": 30000,
          "description": "Timeout in milliseconds for requests (headers and body). Slow or dead hosts abort instead of hanging the agent."
        }
      },
      "additionalProperties": false,
      "layers": [{ "source": "config", "key": "fetchTool" }, { "default": {} }]
    }
  }
}
