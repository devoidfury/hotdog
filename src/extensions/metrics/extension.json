{
  "name": "metrics",
  "provides": [],
  "loadOrder": 10,
  "description": "Export per-run LLM metrics to a CSV file (model, tokens, TTFT, tok/s, memory, workload)",
  "configSchema": {
    "metrics": {
      "type": "object",
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": true,
          "description": "Whether this extension is enabled. Set to false to disable the extension entirely."
        },
        "outputFile": {
          "type": "string",
          "default": null,
          "description": "Path to the CSV file. Defaults to ~/.cache/hotdog/metrics.csv"
        }
      },
      "additionalProperties": false,
      "layers": [
        { "source": "config", "key": "metrics" },
        { "default": {} }
      ]
    }
  }
}
