{
  "name": "profile-switch",
  "provides": [],
  "loadOrder": 10,
  "description": "List and switch profiles at runtime via the /profile slash command",
  "configSchema": {
    "profileSwitch": {
      "type": "object",
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": true,
          "description": "Whether this extension is enabled. Set to false to disable the extension entirely."
        }
      },
      "additionalProperties": false,
      "layers": [{ "source": "config", "key": "profileSwitch" }, { "default": {} }]
    }
  }
}
