{
  "name": "string-length",
  "provides": ["tools"],
  "loadOrder": 1,
  "description": "String length tool — measures a string in UTF-16 code units, Unicode code points, UTF-8 bytes, and graphemes",
  "configSchema": {
    "stringLength": {
      "type": "object",
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": true,
          "description": "Whether this extension is enabled. Set to false to disable the extension entirely."
        }
      },
      "additionalProperties": false,
      "layers": [
        { "source": "config", "key": "stringLength" },
        { "default": {} }
      ]
    }
  }
}
